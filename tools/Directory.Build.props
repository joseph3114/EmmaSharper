<Project>

  <!--
    Deliberately empty, and deliberately NOT importing the repository root props.

    MSBuild stops at the first Directory.Build.props it finds walking up, so this shadows the
    root one. Without it the generator below would inherit <TargetFrameworks>, nullable
    warnings-as-errors and the package metadata, none of which apply to a throwaway tool.
  -->

</Project>
